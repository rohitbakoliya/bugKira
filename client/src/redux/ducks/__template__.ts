// Actions

// Reducer - default export
export default function reducer(state = {}, action: any = {}) {
  switch (action.type) {
    default:
      return state;
  }
}

// Action Creators - export

// Side-effects for thunk
